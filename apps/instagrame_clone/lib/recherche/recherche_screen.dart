import 'package:flutter/material.dart';

class RechercheScreen extends StatelessWidget {
  const RechercheScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(child: Center(child: Text("Recherche")));
  }
}
